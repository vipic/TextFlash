#!/usr/bin/env bash
set -euo pipefail

VERSION=""
if [[ "$#" -gt 0 && "$1" != --* ]]; then
    VERSION="$1"
    shift
else
    VERSION="$(scripts/next_version.sh)"
fi

echo "Publishing version: $VERSION"
exec ./release.sh "$VERSION" --publish "$@"
